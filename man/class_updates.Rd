% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{class_updates}
\alias{class_updates}
\alias{sample_allocation}
\alias{update_s}
\alias{update_z}
\alias{update_m}
\alias{update_classes_wb}
\alias{update_classes_dp}
\title{Update latent classes}
\usage{
sample_allocation(prob)

update_s(delta, m)

update_z(s, beta, b, Omega)

update_m(C, z, non_zero = FALSE)

update_classes_wb(
  s,
  b,
  Omega,
  epsmin = 0.01,
  epsmax = 0.7,
  deltamin = 0.1,
  deltashift = 0.5,
  identify_classes = FALSE,
  Cmax = 10L
)

update_classes_dp(
  beta,
  z,
  b,
  Omega,
  delta,
  mu_b_0,
  Sigma_b_0,
  n_Omega_0,
  V_Omega_0,
  identify_classes = FALSE,
  Cmax = 10L
)
}
\arguments{
\item{prob}{[\code{numeric(C)}]\cr
Class probabilities.}

\item{delta}{[\code{numeric(1)}]\cr
Dirichlet concentration parameter.}

\item{m}{[\code{numeric(C)}]\cr
Class sizes.}

\item{s}{[\code{numeric(C)}]\cr
Class weights.}

\item{beta}{[\code{matrix(P, N)}]\cr
Individual coefficient draws in columns.}

\item{b}{[\code{matrix(P, C)}]\cr
Class means in columns.}

\item{Omega}{[\code{matrix(P * P, C)}]\cr
Vectorized class covariance matrices in columns.}

\item{C}{[\code{integer(1)}]\cr
Number of classes.}

\item{z}{[\code{numeric(N)}]\cr
Class allocations numbered from one to \code{C}.}

\item{non_zero}{[\code{logical(1)}]\cr
Replace empty class sizes by one?}

\item{epsmin}{[\code{numeric(1)}]\cr
Remove the smallest class when its weight is below this threshold.}

\item{epsmax}{[\code{numeric(1)}]\cr
Split the largest class when its weight exceeds this threshold.}

\item{deltamin}{[\code{numeric(1)}]\cr
Merge the two closest classes when the Euclidean distance between their
means is below this threshold.}

\item{deltashift}{[\code{numeric(1)}]\cr
Scale of the mean displacement along the leading covariance eigenvector
after splitting a class.}

\item{identify_classes}{[\code{logical(1)}]\cr
Order the current active classes by size?}

\item{Cmax}{[\code{integer(1)}]\cr
Maximum number of classes.}

\item{mu_b_0}{[\code{numeric(P)}]\cr
Prior mean for class means.}

\item{Sigma_b_0}{[\code{matrix(P, P)}]\cr
Prior covariance for class means.}

\item{n_Omega_0}{[\code{integer(1)}]\cr
Prior degrees of freedom for class covariances.}

\item{V_Omega_0}{[\code{matrix(P, P)}]\cr
Prior scale matrix for class covariances.}
}
\value{
The functions return one sampler update:
\itemize{
\item \code{sample_allocation()}: an integer class label.
\item \code{update_s()}: a \code{C} by 1 numeric matrix of class weights.
\item \code{update_z()}: an \code{N} by 1 numeric matrix of allocations.
\item \code{update_m()}: a \code{C} by 1 numeric matrix of class sizes.
\item \code{update_classes_wb()}: a list with \code{s}, \code{b}, \code{Omega}, and \code{update_type},
where the latter is zero for no update, one for removal, two for
splitting, and three for merging.
\item \code{update_classes_dp()}: a list with \code{z}, \code{b}, \code{Omega}, and \code{C}.
}
}
\description{
Low-level sampler kernels for class weights, allocations, sizes, the
weight-based class update, and Dirichlet-process class updates.
}
\examples{
### a latent class state of six deciders with one random coefficient
set.seed(1)
beta <- matrix(c(-1, -1.2, -0.8, 1, 1.3, 0.9), nrow = 1)
b <- matrix(c(-1, 1), nrow = 1)
Omega <- matrix(c(0.2, 0.2), nrow = 1)

### the weights, the allocations, and the class sizes are drawn in turn
s <- update_s(delta = 1, m = c(3, 3))
z <- update_z(s, beta, b, Omega)
m <- update_m(C = 2, z = z)
sample_allocation(c(0.5, 0.3, 0.2))

### the weight-based update splits a class that grew too large
update_classes_wb(s = c(0.9, 0.1), b = b, Omega = Omega)

### the Dirichlet process update draws the class count from the data
update_classes_dp(
  beta = beta, z = z, b = b, Omega = Omega, delta = 1,
  mu_b_0 = 0, Sigma_b_0 = diag(1), n_Omega_0 = 4, V_Omega_0 = diag(1)
)

}
\references{
\insertRef{Neal2000}{RprobitB}

\insertRef{Oelschlaeger2021}{RprobitB}
}
\keyword{models}
