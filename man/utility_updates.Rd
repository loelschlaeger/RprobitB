% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{utility_updates}
\alias{utility_updates}
\alias{d_to_gamma}
\alias{log_likelihood_ordered}
\alias{update_Sigma}
\alias{update_U}
\alias{update_U_ranked}
\alias{update_d}
\title{Update utilities and thresholds}
\usage{
d_to_gamma(d)

log_likelihood_ordered(d, y, sys, Tvec)

update_Sigma(n_Sigma_0, V_Sigma_0, N, S)

update_U(U, y, sys, Sigma_inv, available = NULL)

update_U_ranked(U, sys, Sigma_inv)

update_d(d, y, sys, log_likelihood, mu_d_0, Sigma_d_0, Tvec, step_scale = 0.1)
}
\arguments{
\item{d}{[\code{numeric(J - 2)}]\cr
Log-increments between finite ordered thresholds.}

\item{y}{[\code{integer(1)} | \code{matrix(N, max(Tvec))}]\cr
Chosen alternative for \code{update_U()}, where \code{J} denotes the base
alternative, or ordered responses by decider and occasion for the
threshold functions.}

\item{sys}{[\code{numeric(J - 1)} | \code{matrix(N, max(Tvec))}]\cr
Systematic utilities matching \code{U} or \code{y}.}

\item{Tvec}{[\code{integer(N)}]\cr
Number of observed occasions for each decider.}

\item{n_Sigma_0}{[\code{integer(1)}]\cr
Prior degrees of freedom for the error covariance.}

\item{V_Sigma_0}{[\code{matrix(J - 1, J - 1)}]\cr
Prior scale matrix for the error covariance.}

\item{N}{[\code{integer(1)}]\cr
Number of independent sampling units.}

\item{S}{[\code{matrix(J - 1, J - 1)}]\cr
Error scatter matrix.}

\item{U}{[\code{numeric(J - 1)}]\cr
Current latent utility differences.}

\item{Sigma_inv}{[\code{matrix(J - 1, J - 1)}]\cr
Inverse error covariance matrix.}

\item{available}{[\code{logical(J)} | \code{NULL}]\cr
Availability of the \code{J - 1} non-base alternatives followed by the base
alternative. Utilities of unavailable alternatives are drawn without
truncation. By default (\code{NULL}), all alternatives are available.}

\item{log_likelihood}{[\code{numeric(1)}]\cr
Current ordered-response log-likelihood.}

\item{mu_d_0}{[\code{numeric(J - 2)}]\cr
Prior mean for threshold log-increments.}

\item{Sigma_d_0}{[\code{matrix(J - 2, J - 2)}]\cr
Prior covariance for threshold log-increments.}

\item{step_scale}{[\code{numeric(1)}]\cr
Random-walk proposal standard deviation.}
}
\value{
The functions return one sampler update or transformation:
\itemize{
\item \code{update_Sigma()}: a \code{J - 1} by \code{J - 1} covariance matrix.
\item \code{update_U()} and \code{update_U_ranked()}: \code{J - 1} by 1 numeric latent
utility matrices.
\item \code{d_to_gamma()}: a numeric column matrix containing ordered thresholds
and their infinite bounds.
\item \code{log_likelihood_ordered()}: one numeric log-likelihood value.
\item \code{update_d()}: a list with updated \code{d} and \code{log_likelihood} values.
}
}
\description{
Low-level Gibbs sampler kernels for error covariance matrices, latent
utilities, and ordered-response thresholds.
}
\examples{
### two deciders who choose twice on an ordered scale of four levels
set.seed(1)
d <- c(0, log(2))
y <- matrix(c(1, 2, 3, 2), nrow = 2)
sys <- matrix(0, nrow = 2, ncol = 2)
Tvec <- c(2, 2)

### the thresholds, their likelihood, and their random-walk update
d_to_gamma(d)
log_likelihood <- log_likelihood_ordered(d, y, sys, Tvec)
update_d(
  d, y, sys, log_likelihood, mu_d_0 = c(0, 0), Sigma_d_0 = diag(2),
  Tvec = Tvec
)

### the latent utilities of an unordered and of a ranked choice
update_U(
  c(0, 0), y = 1, sys = c(0, 0), Sigma_inv = diag(2),
  available = c(TRUE, TRUE, TRUE)
)
update_U_ranked(c(0, 0), sys = c(0, 0), Sigma_inv = diag(2))

### the error covariance
update_Sigma(n_Sigma_0 = 4, V_Sigma_0 = diag(2), N = 10, S = diag(2))

}
\references{
\insertRef{Robert1995}{RprobitB}
}
\keyword{models}
