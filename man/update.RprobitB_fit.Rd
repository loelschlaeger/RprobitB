% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{update.RprobitB_fit}
\alias{update.RprobitB_fit}
\title{Update and refit a choice model}
\usage{
\method{update}{RprobitB_fit}(object, formula., ..., evaluate = TRUE)
}
\arguments{
\item{object}{[\code{RprobitB_fit}]\cr
Fitted choice model.}

\item{formula.}{[\code{formula}]\cr
Changes to the model formula, see the details.}

\item{...}{Arguments of \code{\link[=fit]{fit()}} that replace the ones of \code{object}.}

\item{evaluate}{[\code{logical(1)}]\cr
Refit the model? If \code{FALSE}, the updated call is returned, where \code{data}
stands for the choice data of \code{object}.}
}
\value{
An object of class \code{RprobitB_fit}, or the updated \code{call} if \code{evaluate} is
\code{FALSE}.
}
\description{
Refits a choice model with a modified specification.
}
\details{
Arguments that are not specified are taken from \code{object}.

The model formula is updated part by part, so \code{. ~ . + income} extends the
covariates that are constant across alternatives and leaves the other two
formula parts unchanged.

The choice data of \code{object} are reused, also if they were simulated, which
makes the updated model comparable to \code{object}. Supply \code{data} to fit the
updated model to other choice data.
}
\examples{
### simulate choice data and fit a model with two covariates
set.seed(1)
model <- fit(choice ~ x + y | 0, chains = 1)
summary(model)

### drop `y` from the formula, the other formula parts stay as they are
model_2 <- update(model, . ~ . - y)
summary(model_2)

### let the coefficient of `x` vary across deciders instead
model_3 <- update(model, random_effects = "x")
summary(model_3)
}
\keyword{models}
