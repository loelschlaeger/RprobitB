% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior.R
\name{confint.RprobitB_fit}
\alias{confint.RprobitB_fit}
\title{Compute posterior credible intervals}
\usage{
\method{confint}{RprobitB_fit}(object, parm = NULL, level = 0.95, ...)
}
\arguments{
\item{object}{[\code{RprobitB_fit}]\cr
Fitted choice model.}

\item{parm}{[\code{character()} | \code{NULL}]\cr
Variables to include. \code{NULL} includes every population-level posterior
variable that is not fixed by the normalization or the model structure.
Individual coefficients can be selected by their
\code{individual[effect,decider]} names.}

\item{level}{[\code{numeric(1)}]\cr
Probability of the equal-tailed credible intervals.}

\item{...}{Currently not used.}
}
\value{
A numeric matrix with one row per selected variable and columns for
the lower and upper credible limits.
}
\description{
Computes equal-tailed posterior intervals for selected model variables.
}
\details{
The returned intervals are credible intervals, not confidence intervals.
They are reported through \code{\link[stats:confint]{stats::confint()}} because it is the standard
way to ask a fitted model for interval estimates.
}
\examples{
set.seed(1)
model <- fit(
  choice ~ x | 0, dgp_parameters = list(beta = c(x = 1)), chains = 1
)
confint(model)
}
\keyword{models}
