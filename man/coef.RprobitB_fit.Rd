% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior.R
\name{coef.RprobitB_fit}
\alias{coef.RprobitB_fit}
\title{Extract posterior coefficient summaries}
\usage{
\method{coef}{RprobitB_fit}(
  object,
  type = c("mean", "median"),
  level = c("population", "individual"),
  ...
)
}
\arguments{
\item{object}{[\code{RprobitB_fit}]\cr
Fitted choice model.}

\item{type}{[\code{character(1)}]\cr
The posterior summary to return:
\itemize{
\item \code{"mean"} averages the draws.
\item \code{"median"} is more robust for skewed posteriors.
}}

\item{level}{[\code{character(1)}]\cr
Which parameters to return:
\itemize{
\item \code{"population"} returns the parameters shared by all deciders.
\item \code{"individual"} returns the random coefficients of every decider, which
requires a mixed model fitted with \code{save_individual_draws = TRUE}.
}}

\item{...}{Currently not used.}
}
\value{
For \code{level = "population"}, a named numeric vector with one value per
global posterior variable that is not fixed by the normalization or the model
structure. For \code{level = "individual"}, a numeric matrix with deciders in rows
and random effects in columns. Log-normal coefficients remain on their latent
normal scale.
}
\description{
Returns posterior means or medians for population parameters or individual
random coefficients.
}
\examples{
set.seed(1)
model <- fit(
  choice ~ x | 0,
  random_effects = "x",
  dgp_parameters = list(beta = c(x = 1), Omega = matrix(0.5)),
  chains = 1,
  save_individual_draws = TRUE
)
coef(model)
head(coef(model, level = "individual"))
}
\keyword{models}
