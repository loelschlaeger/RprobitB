% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prediction.R
\name{predict.RprobitB_fit}
\alias{predict.RprobitB_fit}
\title{Predict choices}
\usage{
\method{predict}{RprobitB_fit}(
  object,
  newdata = NULL,
  type = c("population", "conditional"),
  uncertainty = FALSE,
  level = 0.95,
  ghk_draws = 500L,
  progress = interactive(),
  ...
)
}
\arguments{
\item{object}{[\code{RprobitB_fit}]\cr
Fitted choice model.}

\item{newdata}{[\code{data.frame} | \code{NULL}]\cr
Data for prediction. \code{NULL} uses the fitted data. A response column is
optional.}

\item{type}{[\code{character(1)}]\cr
Which coefficients to predict with:
\itemize{
\item \code{"population"} integrates over the estimated population distribution
of the random coefficients and applies to any decider.
\item \code{"conditional"} uses the posterior random coefficients and class
allocations of the deciders that were observed when fitting the model.
}}

\item{uncertainty}{[\code{logical(1)}]\cr
Add posterior standard deviations and credible intervals?}

\item{level}{[\code{numeric(1)}]\cr
Probability of the credible intervals.}

\item{ghk_draws}{[\code{integer(1)}]\cr
Number of draws of the GHK simulator for multivariate normal probabilities
of more than three dimensions, see \code{\link[oeli:pmvnorm]{oeli::pmvnorm()}}.}

\item{progress}{[\code{logical(1)}]\cr
Show progress?}

\item{...}{Currently not used.}
}
\value{
A \code{data.frame} with one row per choice occasion, identifier columns,
\code{.prediction}, and one \verb{probability_*} column per alternative. If
\code{uncertainty = TRUE}, \verb{sd_*}, \verb{lower_*}, and \verb{upper_*} columns are added.
}
\description{
Computes occasion-level choice probabilities over the retained posterior
draws and predicts the alternative with the largest mean probability.
}
\details{
Conditional prediction is based on the individual-level parameters (Train,
2009, Chapters 11 and 12): the posterior distribution of a decider's
coefficients given their observed choices, which the Gibbs sampler
provides as draws when the model is fitted with
\code{save_individual_draws = TRUE}.
}
\examples{
model <- fit(
  choice ~ x | 0,
  random_effects = "x",
  n_deciders = 20,
  iterations = 300,
  warmup = 150,
  chains = 1,
  save_individual_draws = TRUE
)
head(predict(model))
head(predict(model, type = "conditional"))
head(predict(model, uncertainty = TRUE))

### new choice occasions
new_data <- data.frame(deciderID = 21:22, x_A = c(1, -1), x_B = c(0, 0))
predict(model, newdata = new_data)
}
\references{
\insertRef{Train2009}{RprobitB}
}
\keyword{models}
